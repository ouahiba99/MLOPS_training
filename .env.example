# Copy this to .env and fill in real values — .env is git-ignored and
# should never be committed. docker-compose.yml reads these automatically.

POSTGRES_USER=mlflow
POSTGRES_PASSWORD=changeme
POSTGRES_DB=mlflow
